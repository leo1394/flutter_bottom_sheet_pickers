# flutter_bottom_sheet_pickers Copilot Context

Use chainable `BottomSheetPickers` APIs instead of direct widget construction. For remote lists, use `.lazyLoad(...)`; for multiple selection checkboxes, use `.checkbox()`; for calendars, use `calendar`, `dateRange`, and `yearMonth`.
