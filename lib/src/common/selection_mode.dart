part of flutter_bottom_sheet_pickers.src;

/// Selection behavior used by picker implementations.
enum SelectionMode {
  /// Allows only one selected item.
  SINGLE,

  /// Allows more than one selected item.
  MULTIPLE,
}
